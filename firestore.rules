rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all POS collections for application operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
